# 프로젝트 작업 규칙

## 목표

- 이 저장소는 자료구조를 학습하고 직접 적용하는 C 기반 CLI 프로젝트다.
- MVP 범위는 작업 관리, 작업 의존성, 일정 추천, CSV 저장·불러오기로 제한한다.

## 기준 디렉터리 구조

- `docs/00_plan/`: 로드맵, WBS, 의사결정 기록
- `docs/01_learning/`: 개념 정리와 구현 학습 기록
- `docs/02_design/`: ADT, 알고리즘, 모듈, 입출력 설계
- `docs/03_verification/`: 테스트 계획·결과, 복잡도, 메모리 점검
- `include/`: 공개 구조체, 상수, 함수 선언
- `src/`: 구현 코드
- `tests/`: 모듈 단위 테스트
- `data/`: 버전 관리하는 샘플 CSV만 보관

## C 구현 규칙

- 선언은 `.h` 파일에, 구현은 이름이 대응하는 `.c` 파일에 작성한다.
- 헤더 가드, 파일·함수의 `snake_case`, 구조체의 PascalCase, 상수의 UPPER_SNAKE_CASE를 사용한다.
- 동적 메모리를 할당하는 모든 모듈은 대응하는 해제 함수를 제공한다.
- 프로젝트 전체에서 사용할 반환 코드 규칙을 구현 전에 정하고 일관되게 적용한다.
- 핵심 자료구조는 직접 구현하며, 외부 라이브러리로 구현을 대체하지 않는다.

## 검증과 문서화

- 중요한 모듈마다 정상·경계·실패 경로 테스트를 추가한다.
- 실제로 성공한 적 없는 빌드 명령을 동작한다고 문서화하지 않는다.
- 동작을 변경하면 관련 설계·학습·검증 문서를 함께 갱신한다.
- README의 경로, 빌드 명령, 현재 상태 체크 항목을 실제 상태와 일치시킨다.

## 변경 범위

- 명시적인 승인 없이 SQLite, GUI, 웹·서버 기능, 관련 없는 의존성을 추가하지 않는다.
- 생성 산출물, 로컬 에디터 상태, 비공개 테스트 데이터는 커밋하지 않는다.
